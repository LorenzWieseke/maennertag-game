# Hostinger VPS Docker UI erwartet oft diesen Dateinamen (nicht nur .yml).
services:
  maennertag:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-}
